export enum NodeType {
  ellipse = 'ellipse',
  frame = 'frame',
  line = 'line',
  media = 'media',
  polygon = 'polygon',
  rectangle = 'rectangle',
  star = 'star',
}

export enum ToolName {
  comment = 'comment',
  default = 'default',
  ellipse = 'ellipse',
  frame = 'frame',
  line = 'line',
  media = 'media',
  polygon = 'polygon',
  rectangle = 'rectangle',
  star = 'star',
}

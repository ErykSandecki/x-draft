export enum NodeType {
  ellipse = 'ellipse',
  frame = 'frame',
  line = 'line',
  polygon = 'polygon',
  rectangle = 'rectangle',
}

export enum ToolName {
  comment = 'comment',
  default = 'default',
  ellipse = 'ellipse',
  frame = 'frame',
  line = 'line',
  polygon = 'polygon',
  rectangle = 'rectangle',
}

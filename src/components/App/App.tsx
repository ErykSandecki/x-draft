import { FC } from 'react';

// styles
import styles from './App.module.scss';

const App: FC = () => {
  return (
    <main className={styles.App}>
      <h1>x-draft</h1>
    </main>
  );
};

export default App;

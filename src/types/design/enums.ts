export enum NodeType {
  ellipse = 'ellipse',
  frame = 'frame',
  line = 'line',
  rectangle = 'rectangle',
}

export enum ToolName {
  comment = 'comment',
  default = 'default',
  ellipse = 'ellipse',
  frame = 'frame',
  line = 'line',
  rectangle = 'rectangle',
}

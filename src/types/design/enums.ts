export enum NodeType {
  ellipse = 'ellipse',
  frame = 'frame',
  rectangle = 'rectangle',
}

export enum ToolName {
  comment = 'comment',
  default = 'default',
  ellipse = 'ellipse',
  frame = 'frame',
  rectangle = 'rectangle',
}
